on:
  push:
    branches: [main, rc-**]
  pull_request:
    branches: [main]
  schedule:
    - cron:  '0 5 * * 1' # every monday

name: Package checks

jobs:
  R-CMD-check:
    uses: rstudio/shiny-workflows/call-check-pak.yaml@debug
